# wsl1-patch-align
Fix ELF p_align for WSL 1 compatibility

## Python Run
```
python3 fix.py [filepath_or_command]
```
## Install and Run
Install first
```bash
pip install -e .
```
Run
```bash
fixelf [filepath_or_command]
```